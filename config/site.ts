export type SiteConfig = typeof siteConfig;

export const siteConfig = {
	name: "TrackMe",
	description: "My tracker",
	navItems: [
		{
			label: "Home",
			href: "/",
		},
	],
	navMenuItems: [
	],
	links: {},
};
